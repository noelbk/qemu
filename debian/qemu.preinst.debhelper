# Automatically added by dh_installmodules
if [ "$1" = install ] || [ "$1" = upgrade ]; then
	if [ -e "/etc/modprobe.d/qemu" ]; then
		if [ "`md5sum \"/etc/modprobe.d/qemu\" | sed -e \"s/ .*//\"`" = \
		     "`dpkg-query -W -f='${Conffiles}' qemu | sed -n -e \"\\\\' /etc/modprobe.d/qemu '{s/ obsolete$//;s/.* //p}\"`" ]
		then
			rm -f "/etc/modprobe.d/qemu"
		fi
	fi
fi
# End automatically added section
